<div id="favorite-<%= @cafe.id %>">
  <% if !current_user.nil? && current_user.favorite?(@cafe) %>
    <%= render 'users/unfavorite' %>
  <% else %>
    <%= render 'users/favorite' %>
  <% end %>
</div>
